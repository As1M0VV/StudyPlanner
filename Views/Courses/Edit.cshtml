@model Course
@{
    ViewData["Title"] = "Ders Duzenle";
}

<section class="page-title">
    <div>
        <p class="eyebrow">Guncelle</p>
        <h1>Ders Duzenle</h1>
        <p class="text-muted mb-0">Ders bilgilerini guncelle.</p>
    </div>
</section>

<section class="form-panel">
    <form asp-action="Edit" method="post">
        <input type="hidden" asp-for="Id" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="mb-3">
            <label asp-for="Name" class="form-label"></label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Instructor" class="form-label"></label>
            <input asp-for="Instructor" class="form-control" />
            <span asp-validation-for="Instructor" class="text-danger"></span>
        </div>

        <div class="mb-4">
            <label asp-for="Credit" class="form-label"></label>
            <input asp-for="Credit" class="form-control" type="number" min="1" max="10" />
            <span asp-validation-for="Credit" class="text-danger"></span>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Kaydet</button>
            <a class="btn btn-outline-secondary" asp-action="Index">Vazgec</a>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
